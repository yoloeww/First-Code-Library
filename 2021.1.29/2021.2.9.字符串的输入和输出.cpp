# include <stdio.h>
void show (char *a,int len)
{   int i;
   for(i=0;i<len;i++)
   {
     printf("%d\n",a[i]);
   }
}
int main(void)
{
   char a[30],b[30],c[30],ch;
  /* scanf("%s%s%s",a,b,c,&ch);
   printf("%s%s%s",a,b,c,ch);
   show (a,30);
   return 0;*/
/*	gets(a);
	ch=getchar();
	puts(a);
	putchar(ch);  */
   puts("haha");
	return 0;
 
}